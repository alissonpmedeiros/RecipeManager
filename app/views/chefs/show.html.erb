<%= render "shared/page_title", title: "Chef: #{@chef.chefname}"    %>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
	<%= gravatar_for @chef, size: 200 %>
	<hr>
  </div>
</div>

<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
  	<h3><em>Recipes:</em></h3>
  	<%= will_paginate @recipes %>
  </div>
</div>

<%= render 'chefs/recipes', obj: @recipes %>